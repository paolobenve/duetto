{
  "name": "duetto-signaling",
  "version": "0.1.0",
  "private": true,
  "description": "Signalling server for Duetto: it brings the only two peers together and forwards encrypted messages (which the server cannot read).",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test:smoke": "node smoke-test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
