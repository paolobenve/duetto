{
  "extends": "@react-native/typescript-config/tsconfig.json",
  "compilerOptions": {
    "strict": true,
    // React Native's preset includes Jest's types, which are of no use
    // here: the tests live on the server side (server/smoke-test.mjs).
    "types": []
  },
  "include": ["src", "index.js", "*.d.ts"]
}
